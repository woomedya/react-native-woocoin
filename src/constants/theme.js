import * as screenUtil from '../utilities/screen';


export const padding = {
    paddingHorizontal: {
        header: screenUtil.bigWidthScreenCondition(5, 0),
        content: screenUtil.bigWidthScreenCondition(20, 15)
    }
}